#!/usr/bin/env bash

# comments should not break branch coverage
for i in {1..5}; do
	# comments should not break branch coverage
	if [[ $i -eq 4 ]]; then
		# comments should not break branch coverage
		break
		# comments should not break branch coverage
	fi
	# comments should not break branch coverage
done

